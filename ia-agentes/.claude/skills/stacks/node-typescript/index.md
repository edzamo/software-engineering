# Stack Node/TypeScript — índice

Carga `naming.md` (convenciones y `tsconfig` estricto) y `nestjs-hexagonal.md` (estructura, DI con tokens, seguridad, dependency-cruiser). Detecta con `package.json` (`@nestjs/*`, `express`).
