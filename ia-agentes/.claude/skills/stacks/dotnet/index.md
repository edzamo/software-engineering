# Stack .NET — índice

Carga `naming.md` (convenciones y estilo C# 12) y `clean-architecture-dotnet.md` (proyectos, referencias permitidas, EF Core, ASP.NET Core, NetArchTest). Detecta con `*.csproj` (`TargetFramework`).
